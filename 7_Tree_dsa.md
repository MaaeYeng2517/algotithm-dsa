# บทที่ 6 โครงสร้างข้อมูลแบบต้นไม้ (Tree)

โครงสร้างข้อมูลแบบต้นไม้ หรือ **Tree** เป็นโครงสร้างข้อมูลแบบไม่เชิงเส้น (non-linear data structure) ที่จัดเก็บข้อมูลเป็นลำดับชั้น โดยข้อมูลแต่ละส่วนเรียกว่า **โหนด (Node)** และเชื่อมโยงกันด้วย **เส้นเชื่อม (Edge)**

ตัวอย่างโครงสร้างที่มีลักษณะเป็นต้นไม้ ได้แก่ โครงสร้างโฟลเดอร์ในระบบปฏิบัติการ โครงสร้างหมวดหมู่สินค้า และโครงสร้าง DOM ของหน้าเว็บ

## วัตถุประสงค์การเรียนรู้

เมื่อศึกษาเนื้อหานี้แล้ว ผู้เรียนควรสามารถ

1. อธิบายความหมายและองค์ประกอบของโครงสร้างข้อมูลแบบ Tree ได้
2. แยกความแตกต่างระหว่าง Tree, Binary Tree และ Binary Search Tree ได้
3. วิเคราะห์และเขียนลำดับการท่อง Binary Tree แบบ Preorder, Inorder, Postorder และ Level-order ได้
4. อธิบายความซับซ้อนของการค้นหา เพิ่ม ลบ และ Traversal ได้
5. สร้างและใช้งาน Binary Tree เบื้องต้นด้วยภาษา C, C++, Java หรือ Python ได้

## ผลลัพธ์การเรียนรู้

หลังจบบทเรียน ผู้เรียนสามารถ

- ระบุ Root, Parent, Child, Leaf, Depth และ Height จากแผนภาพ Tree ได้
- สร้างโครงสร้างโหนดและเชื่อมโยงโหนดเป็น Binary Tree ได้
- คำนวณผลลัพธ์ของการท่องต้นไม้แต่ละรูปแบบได้อย่างถูกต้อง
- ตรวจสอบว่า Binary Tree ที่กำหนดเป็น Binary Search Tree หรือไม่
- เลือกใช้ Tree ให้เหมาะกับข้อมูลแบบลำดับชั้นและงานค้นหา

## แบบทดสอบก่อนเรียน

ลองตอบคำถามต่อไปนี้ก่อนอ่านเนื้อหา โดยยังไม่ต้องดูเฉลย

1. โหนดบนสุดของ Tree เรียกว่าอะไร
2. โหนดหนึ่งโหนดใน Binary Tree มีโหนดลูกได้มากที่สุดกี่โหนด
3. ถ้า Tree มีโหนด `n` โหนด จะมี Edge กี่เส้น
4. การท่องแบบใดมีลำดับเป็น Left - Root - Right
5. ใน Binary Search Tree ค่าของโหนดในต้นไม้ย่อยด้านซ้ายควรสัมพันธ์กับโหนดปัจจุบันอย่างไร

### เฉลยแบบทดสอบก่อนเรียน

1. Root
2. สองโหนด
3. `n - 1` เส้น
4. Inorder
5. มีค่าน้อยกว่าโหนดปัจจุบัน

## องค์ประกอบสำคัญ

- **Root (ราก)** คือโหนดบนสุดของต้นไม้ และไม่มีโหนดแม่
- **Parent (โหนดแม่)** คือโหนดที่มีโหนดอื่นเชื่อมต่ออยู่ด้านล่าง
- **Child (โหนดลูก)** คือโหนดที่อยู่ถัดลงมาจากโหนดแม่
- **Sibling (โหนดพี่น้อง)** คือโหนดลูกที่มีโหนดแม่เดียวกัน
- **Leaf (ใบ)** คือโหนดที่ไม่มีโหนดลูก
- **Edge (เส้นเชื่อม)** คือความสัมพันธ์ระหว่างโหนดแม่กับโหนดลูก
- **Subtree (ต้นไม้ย่อย)** คือส่วนหนึ่งของต้นไม้ที่ประกอบด้วยโหนดหนึ่งและโหนดลูกทั้งหมดของมัน
- **Depth (ความลึก)** คือจำนวนเส้นเชื่อมจาก Root มายังโหนดนั้น
- **Height (ความสูง)** คือจำนวนเส้นเชื่อมบนเส้นทางที่ยาวที่สุดจากโหนดไปยัง Leaf

## ลักษณะของ Tree

โดยทั่วไป Tree ที่ถูกต้องจะมีคุณสมบัติดังนี้

1. มีโหนด Root ได้เพียงหนึ่งโหนด หากต้นไม้ไม่ว่าง
2. โหนดทุกโหนด ยกเว้น Root มีโหนดแม่ได้เพียงหนึ่งโหนด
3. ไม่มีวงจร (cycle)
4. หากมีโหนดทั้งหมด `n` โหนด จะมีเส้นเชื่อมทั้งหมด `n - 1` เส้น

ตัวอย่างต้นไม้:

```text
        A                 Root
       / \\
      B   C               Children ของ A
     / \\
    D   E                 Leaf
```

## การท่องไปใน Tree

การท่อง (Traversal) คือการเยี่ยมชมโหนดต่าง ๆ ตามลำดับที่กำหนด วิธีที่ใช้บ่อย ได้แก่

### Depth-First Search (DFS)

สำรวจให้ลึกไปตามกิ่งหนึ่งก่อน แล้วจึงย้อนกลับมาสำรวจกิ่งถัดไป วิธีที่เกี่ยวข้องกับ Binary Tree คือ Preorder, Inorder และ Postorder

### Breadth-First Search (BFS)

สำรวจทีละระดับจากบนลงล่าง มักใช้โครงสร้างข้อมูลแบบคิว (Queue) ช่วยในการทำงาน วิธีนี้เรียกอีกอย่างว่า **Level-order traversal**

## Binary Tree

**Binary Tree** หรือ **ไบนารี่ทรี** คือ Tree ที่แต่ละโหนดมีโหนดลูกได้ไม่เกิน 2 โหนด โดยแบ่งเป็น

- **Left child** โหนดลูกด้านซ้าย
- **Right child** โหนดลูกด้านขวา

ตัวอย่าง:

```text
        8
       / \\
      3   10
     / \\
    1   6
```

> Binary Tree ไม่จำเป็นต้องเรียงค่าจากน้อยไปมาก เงื่อนไขการเรียงลำดับจะเป็นคุณสมบัติของ Binary Search Tree

## ตัวอย่างการสร้าง Binary Tree ด้วย Python

```python
class Node:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None


root = Node(8)
root.left = Node(3)
root.right = Node(10)
root.left.left = Node(1)
root.left.right = Node(6)
```

## ตัวอย่างการสร้าง Binary Tree ด้วยภาษา C

```c
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *left;
    struct Node *right;
} Node;

Node *create_node(int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->left = NULL;
    node->right = NULL;
    return node;
}

int main(void) {
    Node *root = create_node(8);
    root->left = create_node(3);
    root->right = create_node(10);
    root->left->left = create_node(1);
    root->left->right = create_node(6);

    return 0;
}
```

## ตัวอย่างการสร้าง Binary Tree ด้วยภาษา C++

```cpp
struct Node {
    int value;
    Node *left;
    Node *right;

    Node(int value) : value(value), left(nullptr), right(nullptr) {}
};

int main() {
    Node *root = new Node(8);
    root->left = new Node(3);
    root->right = new Node(10);
    root->left->left = new Node(1);
    root->left->right = new Node(6);

    return 0;
}
```

## ตัวอย่างการสร้าง Binary Tree ด้วยภาษา Java

```java
class Node {
    int value;
    Node left;
    Node right;

    Node(int value) {
        this.value = value;
    }
}

public class BinaryTreeExample {
    public static void main(String[] args) {
        Node root = new Node(8);
        root.left = new Node(3);
        root.right = new Node(10);
        root.left.left = new Node(1);
        root.left.right = new Node(6);
    }
}
```

## รูปแบบการท่อง Binary Tree

กำหนดต้นไม้ตัวอย่างดังนี้

```text
        8
       / \\
      3   10
     / \\
    1   6
```

### Preorder: Root - Left - Right

เยี่ยมชม Root ก่อน จากนั้น Left และ Right

```text
8, 3, 1, 6, 10
```

### Inorder: Left - Root - Right

เยี่ยมชม Left ก่อน จากนั้น Root และ Right หากใช้กับ Binary Search Tree ผลลัพธ์จะเรียงจากน้อยไปมาก

```text
1, 3, 6, 8, 10
```

### Postorder: Left - Right - Root

เยี่ยมชมโหนดลูกก่อน แล้วจึงเยี่ยมชม Root

```text
1, 6, 3, 10, 8
```

### Level-order: ทีละระดับ

เยี่ยมชมโหนดจากบนลงล่าง และจากซ้ายไปขวา

```text
8, 3, 10, 1, 6
```

## Binary Search Tree (BST)

**Binary Search Tree** เป็น Binary Tree ที่มีเงื่อนไขการจัดเรียงดังนี้

- ค่าทั้งหมดในต้นไม้ย่อยด้านซ้าย น้อยกว่าค่าของโหนดปัจจุบัน
- ค่าทั้งหมดในต้นไม้ย่อยด้านขวา มากกว่าค่าของโหนดปัจจุบัน
- โดยทั่วไปจะไม่อนุญาตให้มีค่าซ้ำ หรือกำหนดกฎสำหรับค่าซ้ำไว้อย่างชัดเจน

การค้นหา เพิ่ม และลบข้อมูลใน BST มีเวลาเฉลี่ยเป็น `O(log n)` เมื่อ Tree มีความสมดุล แต่ถ้าต้นไม้เอียงมาก อาจแย่ลงเป็น `O(n)`

## ความซับซ้อนของการทำงาน

ให้ `n` เป็นจำนวนโหนด และ `h` เป็นความสูงของต้นไม้

| การทำงาน | Tree ทั่วไป | Binary Search Tree โดยทั่วไป | กรณีแย่ที่สุด |
|---|---:|---:|---:|
| ค้นหา | `O(n)` | `O(log n)` | `O(n)` |
| เพิ่มโหนด | ขึ้นอยู่กับรูปแบบ | `O(log n)` | `O(n)` |
| ลบโหนด | ขึ้นอยู่กับรูปแบบ | `O(log n)` | `O(n)` |
| Traversal | `O(n)` | `O(n)` | `O(n)` |

พื้นที่ที่ใช้ในการเก็บโหนดคือ `O(n)` และการทำงานแบบ Recursive ใช้พื้นที่ Stack ประมาณ `O(h)`

## ข้อดีและข้อจำกัด

### ข้อดี

- เหมาะกับข้อมูลที่มีลำดับชั้น
- ค้นหาและจัดเรียงข้อมูลได้มีประสิทธิภาพเมื่อ Tree มีความสมดุล
- เพิ่มและลบข้อมูลได้โดยไม่ต้องเลื่อนข้อมูลทั้งชุดเหมือน Array

### ข้อจำกัด

- การจัดการ Pointer หรือ Reference ซับซ้อนกว่า Array
- หาก Tree ไม่สมดุล ประสิทธิภาพอาจลดลงจนใกล้เคียงกับการค้นหาแบบเชิงเส้น
- ต้องระวัง Stack overflow เมื่อใช้ Recursive กับต้นไม้ที่มีความสูงมาก

## ตัวอย่างการประยุกต์ใช้

- **ระบบไฟล์**: โฟลเดอร์เป็น Parent และไฟล์หรือโฟลเดอร์ย่อยเป็น Child
- **หมวดหมู่สินค้า**: หมวดหมู่หลักเป็น Root และหมวดหมู่ย่อยเป็น Subtree
- **โครงสร้างองค์กร**: ผู้บริหารเป็น Parent และผู้ใต้บังคับบัญชาเป็น Child
- **ฐานข้อมูลดัชนี**: ใช้ต้นไม้ที่สมดุลเพื่อช่วยค้นหาข้อมูลจำนวนมาก

## แบบฝึกหัด

กำหนด Binary Tree ต่อไปนี้

```text
        15
             /  \
      8    20
         / \   / \
    4  12 17  25
```

จงทำแบบฝึกหัดต่อไปนี้

1. ระบุ Root, Leaf และโหนดที่เป็น Sibling
2. ระบุค่า Depth ของโหนด `12` และ Height ของโหนด `8`
3. เขียนลำดับ Preorder, Inorder และ Postorder
4. ตรวจสอบว่าต้นไม้นี้เป็น Binary Search Tree หรือไม่ พร้อมอธิบายเหตุผล
5. หากค้นหาค่า `17` ใน BST จะเปรียบเทียบกับโหนดใดบ้างตามลำดับ

### แนวคำตอบแบบฝึกหัด

1. Root คือ `15`, Leaf คือ `4, 12, 17, 25`, Sibling ได้แก่ `8` กับ `20` และ `4` กับ `12`
2. Depth ของ `12` คือ `2`, Height ของ `8` คือ `1`
3. Preorder: `15, 8, 4, 12, 20, 17, 25`; Inorder: `4, 8, 12, 15, 17, 20, 25`; Postorder: `4, 12, 8, 17, 25, 20, 15`
4. เป็น Binary Search Tree เพราะค่าด้านซ้ายมีค่าน้อยกว่า และค่าด้านขวามีค่ามากกว่าโหนดแม่ทุกระดับ
5. เปรียบเทียบกับ `15` แล้ว `20` แล้ว `17`

## แบบฝึกปฏิบัติการ

### กิจกรรม: สร้างและท่อง Binary Search Tree

ให้ผู้เรียนเขียนโปรแกรมด้วยภาษาใดภาษาหนึ่งจาก C, C++, Java หรือ Python โดยมีความสามารถดังนี้

1. สร้างฟังก์ชันหรือเมธอดสำหรับสร้างโหนด
2. สร้างฟังก์ชัน `insert` เพื่อเพิ่มค่าใน BST
3. รับค่าจำนวนเต็มอย่างน้อย 7 ค่า แล้วสร้างต้นไม้
4. แสดงผลการท่องต้นไม้แบบ Inorder และ Preorder
5. สร้างฟังก์ชัน `search` และทดสอบค้นหาค่าที่มีอยู่กับค่าที่ไม่มีอยู่
6. ระบุจำนวนครั้งที่เปรียบเทียบระหว่างการค้นหาแต่ละครั้ง

ข้อมูลทดสอบแนะนำ:

```text
15, 8, 20, 4, 12, 17, 25
```

ผลลัพธ์ที่ควรได้จาก Inorder:

```text
4, 8, 12, 15, 17, 20, 25
```

### เกณฑ์ประเมินการปฏิบัติการ

| รายการ | คะแนน |
|---|---:|
| สร้าง Node และเชื่อมโยงโหนดถูกต้อง | 2 |
| เพิ่มข้อมูลด้วยกฎของ BST ได้ถูกต้อง | 2 |
| แสดงผล Traversal ได้ถูกต้อง | 2 |
| ค้นหาค่าที่มีและไม่มีได้ถูกต้อง | 2 |
| อธิบายความซับซ้อนและทดสอบโปรแกรม | 2 |
| **รวม** | **10** |

## แบบทดสอบหลังเรียน

1. อธิบายความแตกต่างระหว่าง Binary Tree กับ Binary Search Tree
2. จากต้นไม้ตัวอย่างในแบบฝึกหัด ลำดับ Inorder คืออะไร
3. หาก BST มีความสูง `h` การค้นหามีความซับซ้อนเท่าใด
4. เพราะเหตุใด Tree ที่เอียงมากจึงทำงานได้ใกล้เคียงกับ Linked List
5. หากต้องการลบ Leaf ใน Binary Tree ควรจัดการโหนดนั้นอย่างไร

### เฉลยแบบทดสอบหลังเรียน

1. Binary Tree จำกัดลูกไม่เกินสองโหนด ส่วน BST เพิ่มเงื่อนไขการเรียงค่าด้านซ้ายและด้านขวา
2. `4, 8, 12, 15, 17, 20, 25`
3. โดยทั่วไป `O(log n)` เมื่อสมดุล หรือ `O(h)` และกรณีแย่ที่สุดเป็น `O(n)`
4. เพราะแต่ละโหนดอาจมีลูกต่อกันเป็นสายยาว ทำให้ต้องตรวจทีละโหนด
5. ลบโหนดนั้นออกจากต้นไม้ และปรับ Pointer หรือ Reference ของโหนดแม่ให้เป็น `NULL` หรือ `null`

## สรุปท้ายบท

Tree ใช้แทนความสัมพันธ์แบบลำดับชั้น ส่วน Binary Tree จำกัดให้แต่ละโหนดมีลูกได้ไม่เกินสองโหนด และ Binary Search Tree เพิ่มเงื่อนไขการเรียงค่าเพื่อช่วยให้ค้นหาได้เร็วขึ้น การเลือกใช้ Tree รูปแบบใดควรพิจารณาจากลักษณะข้อมูลและการรับประกันเรื่องความสมดุลของต้นไม้
