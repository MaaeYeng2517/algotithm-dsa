# บทสรุปอัลกอริทึมและโครงสร้างข้อมูล

อัลกอริทึมและโครงสร้างข้อมูลเป็นพื้นฐานสำคัญของการพัฒนาโปรแกรม อัลกอริทึมคือขั้นตอนการแก้ปัญหาอย่างเป็นลำดับ ส่วนโครงสร้างข้อมูลคือวิธีจัดเก็บข้อมูลเพื่อให้การเข้าถึง การค้นหา การเพิ่ม และการลบทำได้เหมาะสมกับงาน

การออกแบบโปรแกรมที่ดีควรพิจารณาทั้งความถูกต้อง ความซับซ้อนด้านเวลา ความซับซ้อนด้านพื้นที่ ขนาดข้อมูล และรูปแบบการใช้งานจริง

## 1. แนวคิดพื้นฐาน

### อัลกอริทึม

อัลกอริทึมที่ดีควรมีจุดเริ่มต้นและจุดสิ้นสุดที่ชัดเจน ขั้นตอนต้องไม่กำกวม รับข้อมูลเข้าได้ตามที่กำหนด และให้ผลลัพธ์ที่ถูกต้อง

ขั้นตอนทั่วไปในการแก้ปัญหา:

1. วิเคราะห์ปัญหาและข้อมูลนำเข้า
2. ออกแบบขั้นตอนวิธี
3. เลือกโครงสร้างข้อมูลที่เหมาะสม
4. เขียนโปรแกรม
5. ทดสอบกรณีปกติ กรณีขอบเขต และกรณีผิดพลาด
6. วิเคราะห์และปรับปรุงประสิทธิภาพ

### ความซับซ้อน Big-O

Big-O ใช้อธิบายอัตราการเติบโตของเวลา หรือพื้นที่ที่อัลกอริทึมต้องใช้เมื่อขนาดข้อมูล `n` เพิ่มขึ้น

| ความซับซ้อน | ความหมายโดยทั่วไป | ตัวอย่าง |
|---|---|---|
| `O(1)` | คงที่ | เข้าถึง Array ด้วยดัชนี, ค้นหา Hash Table โดยเฉลี่ย |
| `O(log n)` | เพิ่มช้า | Binary Search, BST ที่สมดุล |
| `O(n)` | เชิงเส้น | Linear Search, Traversal |
| `O(n log n)` | มีประสิทธิภาพสำหรับข้อมูลขนาดใหญ่ | Merge Sort, Heap Sort |
| `O(n^2)` | เพิ่มเร็วเมื่อข้อมูลมาก | Bubble Sort, Selection Sort |
| `O(2^n)` | เพิ่มเร็วมาก | ปัญหา Recursive บางประเภท |

## 2. โครงสร้างข้อมูลเชิงเส้น

### Array

Array เก็บข้อมูลชนิดเดียวกันในตำแหน่งต่อเนื่องและเข้าถึงด้วยดัชนีได้รวดเร็ว

- เข้าถึงข้อมูล: `O(1)`
- ค้นหาแบบไม่เรียง: `O(n)`
- แทรกหรือลบตรงกลาง: `O(n)`
- เหมาะกับข้อมูลที่ต้องเข้าถึงตามตำแหน่ง

### Linked List

Linked List ประกอบด้วย Node ที่เก็บข้อมูลและ Pointer หรือ Reference ไปยัง Node ถัดไป การเพิ่มหรือลบข้อมูลเมื่อมีตำแหน่งของ Node แล้วทำได้สะดวก แต่การเข้าถึงข้อมูลตามดัชนีต้องเดินทีละ Node

- เข้าถึงตามตำแหน่ง: `O(n)`
- ค้นหา: `O(n)`
- เพิ่มหรือลบเมื่อมีตำแหน่ง: `O(1)`
- เหมาะกับข้อมูลที่มีการเพิ่มและลบบ่อย

### Stack

Stack ทำงานแบบ **LIFO (Last In, First Out)** ข้อมูลที่เพิ่มเข้าหลังสุดจะถูกนำออกก่อน การดำเนินการหลักคือ `push`, `pop` และ `peek` ซึ่งโดยทั่วไปใช้เวลา `O(1)`

การใช้งาน ได้แก่ Undo, การเรียกฟังก์ชันแบบ Recursive, การตรวจวงเล็บ และ DFS แบบใช้ Stack

### Queue

Queue ทำงานแบบ **FIFO (First In, First Out)** ข้อมูลที่เข้าก่อนจะถูกนำออกก่อน การดำเนินการหลักคือ `enqueue` และ `dequeue` ซึ่งโดยทั่วไปใช้เวลา `O(1)` หากออกแบบโครงสร้างเหมาะสม

การใช้งาน ได้แก่ คิวงาน ระบบบริการ และ BFS

## 3. Tree

Tree เป็นโครงสร้างข้อมูลแบบไม่เชิงเส้นที่จัดข้อมูลเป็นลำดับชั้น องค์ประกอบสำคัญคือ Root, Parent, Child, Sibling, Leaf, Edge, Depth และ Height

คุณสมบัติทั่วไป:

- มี Root หนึ่งโหนดเมื่อต้นไม้ไม่ว่าง
- โหนดที่ไม่ใช่ Root มี Parent เพียงหนึ่งโหนด
- ไม่มี Cycle
- ถ้ามี `n` โหนด จะมี Edge `n - 1` เส้น

### Binary Tree และ Binary Search Tree

Binary Tree มีโหนดลูกได้ไม่เกินสองโหนด คือ Left Child และ Right Child ส่วน Binary Search Tree หรือ BST มีเงื่อนไขเพิ่มเติมว่า ค่าด้านซ้ายน้อยกว่าโหนดปัจจุบัน และค่าด้านขวามากกว่าโหนดปัจจุบัน

การท่อง Binary Tree:

- **Preorder**: Root - Left - Right
- **Inorder**: Left - Root - Right
- **Postorder**: Left - Right - Root
- **Level-order**: ท่องทีละระดับด้วย Queue

Inorder ของ BST ที่ถูกต้องจะได้ข้อมูลเรียงจากน้อยไปมาก หาก BST สมดุล การค้นหา เพิ่ม และลบมีเวลาประมาณ `O(log n)` แต่กรณีต้นไม้เอียงมากอาจเป็น `O(n)`

## 4. Graph

Graph ใช้แทนความสัมพันธ์ระหว่าง Vertex และ Edge โดยอาจเป็นกราฟมีทิศทาง ไม่มีทิศทาง มีน้ำหนัก หรือไม่มีน้ำหนัก

คำสำคัญ:

- **Degree**: จำนวน Edge ที่เชื่อมกับ Vertex
- **In-degree/Out-degree**: จำนวน Edge ที่เข้า/ออกใน Directed Graph
- **Path**: ลำดับ Vertex ที่เชื่อมต่อกัน
- **Cycle**: Path ที่ย้อนกลับมายังจุดเริ่มต้น
- **Connected Graph**: กราฟที่สามารถเดินทางระหว่างจุดต่าง ๆ ได้ตามเงื่อนไข

### วิธีแทน Graph

| วิธี | พื้นที่ | ตรวจสอบ Edge | เหมาะกับ |
|---|---:|---:|---|
| Adjacency Matrix | `O(V^2)` | `O(1)` | กราฟหนาแน่น |
| Adjacency List | `O(V + E)` | `O(V)` โดยทั่วไป | กราฟเบาบาง |

### BFS และ DFS

- **BFS** ใช้ Queue และสำรวจทีละระดับ เหมาะกับการหาเส้นทางที่มีจำนวน Edge น้อยที่สุดใน Unweighted Graph
- **DFS** ใช้ Stack หรือ Recursive และสำรวจให้ลึกก่อน เหมาะกับการตรวจ Cycle การตรวจการเชื่อมต่อ และการค้นหาเชิงลึก
- ทั้ง BFS และ DFS มีความซับซ้อน `O(V + E)` เมื่อใช้ Adjacency List

### อัลกอริทึม Graph ที่สำคัญ

- **Dijkstra** หาเส้นทางสั้นที่สุดใน Weighted Graph ที่ไม่มีน้ำหนักติดลบ
- **Bellman-Ford** รองรับน้ำหนักติดลบและตรวจ Negative Cycle ได้
- **Floyd-Warshall** หาเส้นทางสั้นที่สุดระหว่างทุกคู่ Vertex
- **Kruskal และ Prim** หา Minimum Spanning Tree ในกราฟที่เหมาะสม

## 5. Sorting

Sorting คือการจัดข้อมูลตามลำดับ เช่น Ascending หรือ Descending การเรียงข้อมูลช่วยให้ค้นหาและวิเคราะห์ข้อมูลได้ง่ายขึ้น

| อัลกอริทึม | Best | Average | Worst | Space | Stable |
|---|---:|---:|---:|---:|:---:|
| Bubble Sort | `O(n)`* | `O(n^2)` | `O(n^2)` | `O(1)` | ใช่ |
| Selection Sort | `O(n^2)` | `O(n^2)` | `O(n^2)` | `O(1)` | ไม่เสมอไป |
| Insertion Sort | `O(n)` | `O(n^2)` | `O(n^2)` | `O(1)` | ใช่ |
| Merge Sort | `O(n log n)` | `O(n log n)` | `O(n log n)` | `O(n)` | ใช่ |
| Quick Sort | `O(n log n)` | `O(n log n)` | `O(n^2)` | `O(log n)` โดยเฉลี่ย | ไม่ |
| Heap Sort | `O(n log n)` | `O(n log n)` | `O(n log n)` | `O(1)` | ไม่ |

`*` Bubble Sort จะมี Best Case `O(n)` เมื่อมีการตรวจสอบและหยุดได้เมื่อไม่มีการสลับข้อมูล

แนวทางเลือกใช้:

- ใช้ Insertion Sort กับข้อมูลจำนวนน้อยหรือเกือบเรียงแล้ว
- ใช้ Merge Sort เมื่อต้องการ `O(n log n)` ที่สม่ำเสมอและ Stable
- ใช้ Quick Sort เมื่อประสิทธิภาพเฉลี่ยและพื้นที่มีความสำคัญ
- ใช้ Heap Sort เมื่อต้องการรับประกัน Worst Case `O(n log n)` และใช้พื้นที่น้อย
- งานจริงควรพิจารณาฟังก์ชัน Sorting ของไลบรารีมาตรฐานก่อนเขียนเอง

## 6. Hash Table

Hash Table เก็บข้อมูลเป็นคู่ Key-Value โดยใช้ Hash Function แปลง Key เป็นตำแหน่งในตาราง

องค์ประกอบสำคัญ:

- **Hash Function**: แปลง Key เป็นดัชนี
- **Collision**: Key ต่างกันได้ดัชนีเดียวกัน
- **Load Factor**: จำนวนข้อมูลหารด้วยจำนวนช่อง
- **Rehashing**: ขยายตารางและคำนวณตำแหน่งข้อมูลใหม่

วิธีจัดการ Collision:

- **Separate Chaining**: เก็บหลายรายการใน Slot เดียวผ่าน Linked List หรือรายการ
- **Linear Probing**: ค้นหา Slot ถัดไปทีละช่อง
- **Quadratic Probing**: เพิ่มระยะค้นหาเป็นกำลังสอง
- **Double Hashing**: ใช้ Hash Function สองตัวเพื่อกระจายตำแหน่ง

| การดำเนินการ | Average Case | Worst Case |
|---|---:|---:|
| Search | `O(1)` | `O(n)` |
| Insert | `O(1)` | `O(n)` |
| Delete | `O(1)` | `O(n)` |

Hash Table เหมาะกับ Dictionary, Map, Cache, การนับความถี่ และการตรวจสอบข้อมูลซ้ำ แต่ไม่เหมาะกับงานที่ต้องรักษาลำดับข้อมูลหรือค้นหาตามช่วงค่า

## 7. อัลกอริทึมการค้นหา

### Linear Search

ตรวจสอบข้อมูลตั้งแต่ต้นจนพบค่าที่ต้องการหรือหมดรายการ ใช้ได้กับข้อมูลที่ยังไม่เรียงและมีความซับซ้อน `O(n)`

### Binary Search

ใช้กับข้อมูลที่เรียงแล้วเท่านั้น โดยแบ่งช่วงค้นหาออกเป็นครึ่งหนึ่งทุกครั้ง มีความซับซ้อน `O(log n)` แต่การแทรกข้อมูลใน Sorted Array อาจมีค่าใช้จ่าย `O(n)`

### Hash Search

ใช้ Hash Function คำนวณตำแหน่งจาก Key โดยมีเวลาเฉลี่ย `O(1)` แต่ไม่รับประกันลำดับและมี Worst Case `O(n)`

## 8. การเลือกโครงสร้างข้อมูลให้เหมาะกับงาน

| ความต้องการ | โครงสร้างหรืออัลกอริทึมที่เหมาะสม |
|---|---|
| เข้าถึงข้อมูลตามตำแหน่ง | Array |
| เพิ่มและลบข้อมูลในลำดับ | Linked List |
| ทำงานแบบเข้าหลังออกก่อน | Stack |
| ทำงานแบบเข้าก่อนออกก่อน | Queue |
| จัดข้อมูลเป็นลำดับชั้น | Tree |
| ค้นหาในข้อมูลที่เรียงแล้ว | Binary Search |
| ค้นหาด้วย Key อย่างรวดเร็ว | Hash Table |
| แทนความสัมพันธ์และเครือข่าย | Graph |
| เรียงข้อมูลขนาดใหญ่ | Merge Sort, Quick Sort หรือ Heap Sort |
| หาเส้นทางสั้นที่สุดแบบไม่มีน้ำหนัก | BFS |
| หาเส้นทางสั้นที่สุดแบบมีน้ำหนักไม่ติดลบ | Dijkstra |

## 9. หลักการวิเคราะห์และทดสอบ

ควรทดสอบอัลกอริทึมด้วยข้อมูลหลายลักษณะ:

- ข้อมูลว่าง
- ข้อมูลมีเพียงหนึ่งรายการ
- ข้อมูลมีค่าซ้ำ
- ข้อมูลเรียงแล้ว
- ข้อมูลเรียงย้อนกลับ
- ข้อมูลจำนวนมาก
- Key หรือ Vertex ที่ไม่มีอยู่
- กราฟที่มี Cycle และกราฟที่ไม่ Connected

ควรตรวจสอบทั้งผลลัพธ์ ความถูกต้อง เวลาใช้งาน การใช้หน่วยความจำ และพฤติกรรมกรณีขอบเขต ไม่ควรสรุปประสิทธิภาพจากข้อมูลทดสอบเพียงชุดเดียว

## 10. สรุปภาพรวม

โครงสร้างข้อมูลเป็นเครื่องมือจัดเก็บข้อมูล ส่วนอัลกอริทึมเป็นขั้นตอนการประมวลผลข้อมูล การเลือกใช้ทั้งสองอย่างต้องพิจารณาความสัมพันธ์ระหว่างข้อมูลและการดำเนินการที่เกิดขึ้นบ่อย

Array เหมาะกับการเข้าถึงตามตำแหน่ง Stack และ Queue เหมาะกับงานที่มีลำดับการเข้าออก Tree เหมาะกับข้อมูลแบบลำดับชั้น Graph เหมาะกับความสัมพันธ์ที่ซับซ้อน Hash Table เหมาะกับการค้นหาด้วย Key และ Sorting ช่วยจัดข้อมูลให้พร้อมสำหรับการค้นหาและการวิเคราะห์

ไม่มีโครงสร้างข้อมูลหรืออัลกอริทึมใดดีที่สุดสำหรับทุกปัญหา วิธีที่เหมาะสมที่สุดคือวิธีที่ให้ผลลัพธ์ถูกต้อง ใช้ทรัพยากรเหมาะสม และสอดคล้องกับรูปแบบข้อมูลและข้อจำกัดของระบบ

## แบบทบทวนท้ายบท

1. อธิบายความแตกต่างระหว่างโครงสร้างข้อมูลเชิงเส้นและไม่เชิงเส้น
2. เปรียบเทียบ Array, Linked List, Stack และ Queue
3. เพราะเหตุใด Inorder ของ BST จึงให้ข้อมูลเรียงลำดับ
4. เปรียบเทียบ BFS กับ DFS ในด้านโครงสร้างข้อมูลที่ใช้และลักษณะการค้นหา
5. อธิบาย Collision และวิธีจัดการใน Hash Table
6. หากต้องการค้นหาข้อมูลใน Array ที่เรียงแล้ว ควรใช้อัลกอริทึมใด
7. หากต้องการหาเส้นทางสั้นที่สุดใน Unweighted Graph ควรใช้อัลกอริทึมใด
8. ปัจจัยใดบ้างที่ควรพิจารณาก่อนเลือกใช้อัลกอริทึม

### แนวคำตอบแบบทบทวน

1. โครงสร้างเชิงเส้นจัดข้อมูลเป็นลำดับต่อเนื่อง ส่วนไม่เชิงเส้นมีความสัมพันธ์แบบลำดับชั้นหรือเครือข่าย
2. Array เข้าถึงตามดัชนีเร็ว, Linked List เพิ่มลบสะดวก, Stack เป็น LIFO และ Queue เป็น FIFO
3. เพราะ BST กำหนดค่าด้านซ้ายน้อยกว่าและด้านขวามากกว่าโหนดปัจจุบัน
4. BFS ใช้ Queue และค้นหาทีละระดับ ส่วน DFS ใช้ Stack หรือ Recursive และค้นหาเชิงลึก
5. Collision คือหลาย Key ได้ตำแหน่งเดียวกัน จัดการด้วย Chaining หรือ Open Addressing
6. Binary Search
7. BFS
8. ขนาดข้อมูล รูปแบบข้อมูล การดำเนินการที่ใช้บ่อย เวลา หน่วยความจำ ความเสถียร และกรณีแย่ที่สุด
