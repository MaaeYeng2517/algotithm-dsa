# โครงสร้างข้อมูลแบบตารางแฮช (Hash Table)

**Hash Table** หรือตารางแฮช เป็นโครงสร้างข้อมูลที่ใช้เก็บข้อมูลในรูปแบบคู่ **คีย์ (Key)** และ **ค่า (Value)** โดยใช้ฟังก์ชันแฮชแปลงคีย์ให้เป็นตำแหน่งในตาราง ทำให้สามารถเพิ่ม ค้นหา และลบข้อมูลได้อย่างรวดเร็วโดยเฉลี่ย

ตัวอย่างการใช้งาน ได้แก่ พจนานุกรม ระบบเก็บข้อมูลผู้ใช้ ตารางสัญลักษณ์ของคอมไพเลอร์ แคช และการนับความถี่ของข้อมูล

## วัตถุประสงค์การเรียนรู้

เมื่อศึกษาเนื้อหานี้แล้ว ผู้เรียนควรสามารถ

1. อธิบายความหมายและองค์ประกอบของ Hash Table ได้
2. อธิบายการทำงานของ Hash Function และ Collision ได้
3. เปรียบเทียบวิธีจัดการ Collision แบบ Chaining และ Open Addressing ได้
4. วิเคราะห์ความซับซ้อนของการเพิ่ม ค้นหา และลบข้อมูลได้
5. สร้างและใช้งาน Hash Table เบื้องต้นด้วยภาษา C, C++, Java หรือ Python ได้

## ผลลัพธ์การเรียนรู้

หลังจบบทเรียน ผู้เรียนสามารถ

- คำนวณตำแหน่งจัดเก็บจากคีย์ด้วย Hash Function ได้
- อธิบายสาเหตุที่ทำให้เกิด Collision และเลือกวิธีแก้ไขได้
- ใช้ Hash Table เก็บข้อมูลแบบ Key-Value ได้
- วิเคราะห์ผลของ Load Factor ต่อประสิทธิภาพได้
- เลือกใช้ Hash Table ให้เหมาะกับปัญหาการค้นหาข้อมูล

## แบบทดสอบก่อนเรียน

เลือกคำตอบที่ถูกต้องที่สุด โดยยังไม่ต้องดูเฉลย

1. Hash Table ใช้เก็บข้อมูลในรูปแบบใด

    - ก. คู่ Key-Value
    - ข. โหนดที่มีลูกไม่เกินสองโหนด
    - ค. ลำดับของ Vertex และ Edge
    - ง. ข้อมูลที่เรียงจากน้อยไปมากเท่านั้น

2. หน้าที่หลักของ Hash Function คืออะไร

    - ก. เรียงข้อมูลตามลำดับ
    - ข. แปลง Key ให้เป็นตำแหน่งในตาราง
    - ค. ลบข้อมูลที่ซ้ำกัน
    - ง. เพิ่มจำนวนช่องในตารางทุกครั้ง

3. Collision เกิดขึ้นเมื่อใด

    - ก. ตารางไม่มีข้อมูลเหลืออยู่
    - ข. Key ต่างกันถูกแปลงไปยังตำแหน่งเดียวกัน
    - ค. ข้อมูลถูกเรียงผิดลำดับ
    - ง. มีการลบ Value ออกจากตาราง

4. โครงสร้างข้อมูลใดเหมาะสำหรับใช้จัดการ Collision แบบ Chaining

    - ก. Linked List
    - ข. Stack เท่านั้น
    - ค. Binary Tree เท่านั้น
    - ง. Queue เท่านั้น

5. การค้นหาใน Hash Table มีเวลาเฉลี่ยเท่าใดเมื่อ Hash Function ทำงานได้ดี

    - ก. `O(1)`
    - ข. `O(log n)`
    - ค. `O(n)`
    - ง. `O(n^2)`

### เฉลยแบบทดสอบก่อนเรียน

1. ก
2. ข
3. ข
4. ก
5. ก

## องค์ประกอบของ Hash Table

- **Key** คือข้อมูลที่ใช้ระบุรายการ เช่น รหัสนักศึกษา หรือชื่อผู้ใช้
- **Value** คือข้อมูลที่สัมพันธ์กับ Key เช่น ชื่อนักศึกษา หรือข้อมูลบัญชี
- **Hash Function** คือฟังก์ชันที่แปลง Key เป็นค่าดัชนีของตาราง
- **Bucket หรือ Slot** คือช่องสำหรับเก็บข้อมูลในตาราง
- **Collision** คือเหตุการณ์ที่ Key หลายค่าถูกแปลงไปยัง Slot เดียวกัน
- **Load Factor** คือสัดส่วนระหว่างจำนวนข้อมูลกับขนาดของตาราง

สูตร Load Factor คือ

```text
Load Factor = จำนวนข้อมูล / จำนวนช่องในตาราง
```

หากมีข้อมูล `n` รายการและตารางมี `m` ช่อง ค่า Load Factor คือ `n / m` เมื่อค่าเข้าใกล้ 1 โอกาสเกิด Collision มักเพิ่มขึ้น และอาจต้องขยายตาราง

## Hash Function

Hash Function ที่ดีควรมีคุณสมบัติดังนี้

1. ให้ผลลัพธ์อยู่ในช่วงดัชนีของตาราง
2. คำนวณได้รวดเร็ว
3. กระจาย Key ไปยังช่องต่าง ๆ อย่างสม่ำเสมอ
4. ให้ผลลัพธ์เดิมเมื่อได้รับ Key เดิม
5. ลดโอกาสการเกิด Collision

ตัวอย่าง Hash Function แบบ Division Method:

```text
hash(key) = key mod table_size
```

ถ้าตารางมี 10 ช่อง และ Key เท่ากับ 37 จะได้ตำแหน่งดังนี้

```text
37 mod 10 = 7
```

ดังนั้นข้อมูลจะถูกจัดเก็บที่ช่องหมายเลข 7

> Hash Function ไม่จำเป็นต้องทำให้ Key ต่างกันทุกคู่ได้ตำแหน่งต่างกัน เพราะจำนวน Key อาจมากกว่าจำนวนช่องในตาราง จึงต้องมีวิธีจัดการ Collision

## Collision

Collision เกิดขึ้นเมื่อ Key ตั้งแต่สองค่าขึ้นไปถูก Hash ไปยัง Slot เดียวกัน ตัวอย่างเช่น ใช้ `hash(key) = key mod 10`

```text
23 mod 10 = 3
43 mod 10 = 3
```

ทั้ง Key `23` และ `43` จึงชนกันที่ช่องหมายเลข 3

วิธีจัดการ Collision ที่ใช้บ่อยมีสองแบบคือ Chaining และ Open Addressing

## Separate Chaining

Separate Chaining เก็บรายการหลายรายการไว้ใน Slot เดียวกัน โดยแต่ละ Slot มักชี้ไปยัง Linked List หรือรายการข้อมูล

ตัวอย่าง:

```text
ช่อง 0: ว่าง
ช่อง 1: (21, "Ann") -> (31, "Bob")
ช่อง 2: ว่าง
ช่อง 3: (13, "Cat")
```

### ข้อดี

- จัดการ Collision ได้ง่าย
- ตารางสามารถเก็บข้อมูลมากกว่าจำนวนช่องได้
- ลบข้อมูลได้สะดวก

### ข้อจำกัด

- ต้องใช้พื้นที่เพิ่มเติมสำหรับ Linked List หรือรายการ
- หาก Hash Function ไม่ดี รายการในบางช่องอาจยาวมาก
- การค้นหาในช่องที่มี Collision มากอาจช้าลง

## Open Addressing

Open Addressing จัดเก็บข้อมูลทั้งหมดไว้ภายในตาราง เมื่อเกิด Collision จะค้นหา Slot อื่นที่ว่างตามลำดับที่กำหนด

### Linear Probing

ตรวจสอบช่องถัดไปทีละช่องจนพบช่องว่าง

```text
index = (hash(key) + i) mod table_size
```

เมื่อ `i` เป็น `0, 1, 2, ...`

ข้อเสียคืออาจเกิด **Primary Clustering** หรือการรวมตัวของข้อมูลเป็นกลุ่มยาว ทำให้ค้นหาช้าลง

### Quadratic Probing

ตรวจสอบตำแหน่งโดยเพิ่มระยะห่างเป็นกำลังสอง

```text
index = (hash(key) + i^2) mod table_size
```

ช่วยลด Primary Clustering แต่ยังอาจเกิด Secondary Clustering ได้

### Double Hashing

ใช้ Hash Function สองตัวเพื่อคำนวณระยะกระโดดในการค้นหา Slot ใหม่

```text
index = (hash1(key) + i * hash2(key)) mod table_size
```

โดยทั่วไปช่วยกระจายตำแหน่งได้ดีกว่า Linear Probing และ Quadratic Probing

## การดำเนินการพื้นฐาน

### Insert

1. คำนวณตำแหน่งจาก Key ด้วย Hash Function
2. ตรวจสอบว่าช่องว่างหรือมีข้อมูลเดิมอยู่หรือไม่
3. หากเกิด Collision ให้ใช้ Chaining หรือ Probing
4. เก็บคู่ Key-Value ลงในตาราง

### Search

1. คำนวณตำแหน่งจาก Key
2. ตรวจสอบข้อมูลในช่องนั้น
3. หากใช้ Chaining ให้ค้นหาในรายการของช่องนั้น
4. หากใช้ Open Addressing ให้ตรวจสอบตามลำดับ Probing

### Delete

การลบแบบ Chaining สามารถลบรายการจาก Linked List ได้โดยตรง ส่วน Open Addressing ไม่ควรเปลี่ยนช่องที่ลบเป็นว่างทันที เพราะอาจทำให้การค้นหาข้อมูลที่อยู่ถัดไปหยุดก่อนเวลา ควรใช้สถานะ **Deleted** หรือทำการจัดข้อมูลใหม่ตามวิธีที่เหมาะสม

### Resize และ Rehash

เมื่อ Load Factor สูงเกินค่าที่กำหนด เช่น `0.7` อาจขยายตารางให้ใหญ่ขึ้น แล้วคำนวณตำแหน่งของข้อมูลทุกรายการใหม่ กระบวนการนี้เรียกว่า **Rehashing** และอาจใช้เวลา `O(n)` ในครั้งนั้น แต่ช่วยให้การทำงานในระยะยาวมีประสิทธิภาพดีขึ้น

## ความซับซ้อนของ Hash Table

| การดำเนินการ | Average Case | Worst Case |
|---|---:|---:|
| ค้นหา | `O(1)` | `O(n)` |
| เพิ่มข้อมูล | `O(1)` | `O(n)` |
| ลบข้อมูล | `O(1)` | `O(n)` |

กรณีแย่ที่สุดเกิดจาก Hash Function กระจายข้อมูลไม่ดีจนข้อมูลจำนวนมากอยู่ใน Slot เดียวกัน หรือ Open Addressing มีช่องว่างเหลือน้อยมาก

## ตัวอย่างการใช้งานด้วยภาษา Python

```python
class HashTable:
    def __init__(self, size=10):
        self.table = [[] for _ in range(size)]

    def _index(self, key):
        return hash(key) % len(self.table)

    def set(self, key, value):
        bucket = self.table[self._index(key)]
        for index, (stored_key, _) in enumerate(bucket):
            if stored_key == key:
                bucket[index] = (key, value)
                return
        bucket.append((key, value))

    def get(self, key):
        bucket = self.table[self._index(key)]
        for stored_key, value in bucket:
            if stored_key == key:
                return value
        return None


users = HashTable()
users.set("student01", "Somchai")
users.set("student02", "Suda")
print(users.get("student01"))
```

## ตัวอย่างการใช้งานด้วยภาษา C

```c
#include <stdio.h>

#define TABLE_SIZE 10

int hash(int key) {
    return key % TABLE_SIZE;
}

int main(void) {
    int table[TABLE_SIZE] = {0};
    int key = 37;
    int index = hash(key);

    table[index] = key;
    printf("Key %d is stored at index %d\n", key, index);
    return 0;
}
```

> ตัวอย่างภาษา C นี้สาธิตการคำนวณตำแหน่งและการเก็บข้อมูลแบบง่าย หากใช้งานจริงควรเพิ่มโครงสร้างสำหรับจัดการ Collision และตรวจสอบช่องว่าง

## ตัวอย่างการใช้งานด้วยภาษา C++

```cpp
#include <iostream>
#include <unordered_map>
#include <string>

int main() {
    std::unordered_map<std::string, int> scores;
    scores["Somchai"] = 85;
    scores["Suda"] = 92;

    std::cout << scores["Suda"] << "\n";
    return 0;
}
```

## ตัวอย่างการใช้งานด้วยภาษา Java

```java
import java.util.HashMap;
import java.util.Map;

public class HashTableExample {
    public static void main(String[] args) {
        Map<String, Integer> scores = new HashMap<>();
        scores.put("Somchai", 85);
        scores.put("Suda", 92);

        System.out.println(scores.get("Suda"));
    }
}
```

## Hash Table กับโครงสร้างข้อมูลอื่น

| โครงสร้างข้อมูล | ค้นหาโดยเฉลี่ย | รักษาลำดับข้อมูล | เหมาะกับ |
|---|---:|:---:|---|
| Array ที่ไม่เรียง | `O(n)` | ได้ | ข้อมูลที่เข้าถึงด้วยดัชนี |
| Sorted Array | `O(log n)` | ได้ | ข้อมูลที่ต้องการค้นหาแบบ Binary Search |
| Hash Table | `O(1)` | ไม่รับประกัน | การค้นหาด้วย Key |
| Balanced BST | `O(log n)` | ได้ตามลำดับ | ข้อมูลที่ต้องการลำดับและการค้นหา |

Hash Table เหมาะเมื่อสนใจการค้นหาด้วย Key อย่างรวดเร็ว แต่ไม่เหมาะเมื่อจำเป็นต้องวนข้อมูลตามลำดับที่เรียงแล้ว

## การประยุกต์ใช้

- **Dictionary หรือ Map**: จับคู่คำกับความหมายหรือรหัสกับข้อมูล
- **ฐานข้อมูล**: สร้างดัชนีเพื่อค้นหารายการด้วยคีย์
- **Cache**: เก็บผลลัพธ์ที่เรียกใช้บ่อย
- **การนับความถี่**: นับจำนวนครั้งที่คำหรือค่าปรากฏ
- **ตรวจสอบข้อมูลซ้ำ**: ใช้ Key เป็นตัวระบุข้อมูลที่เคยพบแล้ว
- **Symbol Table**: เก็บชื่อและชนิดของตัวแปรในคอมไพเลอร์

## แบบฝึกหัด

กำหนด Hash Function เป็น `hash(key) = key mod 7` และมี Key ดังนี้

```text
10, 17, 24, 5, 12
```

จงทำแบบฝึกหัดต่อไปนี้

1. คำนวณตำแหน่งของ Key ทุกค่า
2. ระบุว่ามี Collision เกิดขึ้นที่ตำแหน่งใด
3. แสดงผลการเก็บข้อมูลด้วย Separate Chaining
4. หากใช้ Linear Probing จงแสดงตำแหน่งใหม่ของข้อมูลที่เกิด Collision
5. คำนวณ Load Factor หลังเก็บข้อมูลครบทุกค่า

### แนวคำตอบแบบฝึกหัด

1. `10 -> 3`, `17 -> 3`, `24 -> 3`, `5 -> 5`, `12 -> 5`
2. เกิด Collision ที่ตำแหน่ง `3` และ `5`
3. ช่อง 3 เก็บ `10 -> 17 -> 24` และช่อง 5 เก็บ `5 -> 12`
4. หากใช้ Linear Probing ตามลำดับที่กำหนด: `10 -> 3`, `17 -> 4`, `24 -> 5`, `5 -> 6`, `12 -> 0`
5. Load Factor คือ `5 / 7` หรือประมาณ `0.71`

## แบบฝึกหัดแบบมีตัวเลือก

เลือกคำตอบที่ถูกต้องที่สุด

1. Hash Table เก็บข้อมูลในลักษณะใด

   - ก. โหนดที่มีลูกไม่เกินสองโหนด
   - ข. คู่ Key-Value
   - ค. ข้อมูลที่ต้องเรียงจากน้อยไปมากเสมอ
   - ง. ข้อมูลแบบกราฟเท่านั้น

2. หน้าที่ของ Hash Function คืออะไร

   - ก. ลบข้อมูลที่ซ้ำกัน
   - ข. แปลง Key ให้เป็นตำแหน่งในตาราง
   - ค. เรียงข้อมูลทุกค่า
   - ง. เพิ่มขนาดข้อมูล

3. Collision หมายถึงอะไร

   - ก. ตารางไม่มีช่องว่างเหลือ
   - ข. Key สองค่าขึ้นไปได้ตำแหน่งเดียวกัน
   - ค. ข้อมูลถูกเรียงผิดลำดับ
   - ง. การลบข้อมูลจากตาราง

4. วิธีใดเป็นการจัดการ Collision แบบ Chaining

   - ก. เก็บหลายรายการไว้ในรายการเดียวกันของ Slot
   - ข. สลับข้อมูลทุกคู่
   - ค. แบ่งอาร์เรย์เป็นสองส่วน
   - ง. ใช้ Stack เท่านั้น

5. การค้นหาใน Hash Table โดยเฉลี่ยมีความซับซ้อนเท่าใด

   - ก. `O(1)`
   - ข. `O(log n)`
   - ค. `O(n)`
   - ง. `O(n^2)`

6. เมื่อ Load Factor สูงขึ้น ผลที่มักเกิดขึ้นคืออะไร

   - ก. Collision ลดลง
   - ข. Collision เพิ่มขึ้นและการค้นหาอาจช้าลง
   - ค. Key หายไปทั้งหมด
   - ง. ตารางเรียงข้อมูลเอง

7. การขยายตารางและคำนวณตำแหน่งข้อมูลใหม่เรียกว่าอะไร

   - ก. Traversal
   - ข. Rehashing
   - ค. Partitioning
   - ง. Recursion

8. ข้อใดเป็นข้อจำกัดของ Hash Table

   - ก. ค้นหาด้วย Key ได้รวดเร็ว
   - ข. ไม่รับประกันลำดับข้อมูล
   - ค. รองรับคู่ Key-Value
   - ง. เพิ่มข้อมูลได้โดยใช้ Hash Function

### เฉลยแบบฝึกหัดแบบมีตัวเลือก

1. ข
2. ข
3. ข
4. ก
5. ก
6. ข
7. ข
8. ข

## แบบฝึกปฏิบัติการ

### กิจกรรม: สร้าง Hash Table ด้วย Chaining

ให้ผู้เรียนเขียนโปรแกรมด้วยภาษาใดภาษาหนึ่งจาก C, C++, Java หรือ Python โดยมีความสามารถดังนี้

1. สร้างตารางแฮชขนาดอย่างน้อย 7 ช่อง
2. สร้าง Hash Function ด้วยวิธี Division Method
3. เพิ่มคู่ Key-Value และจัดการ Collision ด้วย Chaining
4. ค้นหาค่า Value จาก Key
5. ลบข้อมูลจาก Key ที่กำหนด
6. แสดงข้อมูลในทุก Slot และคำนวณ Load Factor

ข้อมูลทดสอบแนะนำ:

```text
(10, "A"), (17, "B"), (24, "C"), (5, "D"), (12, "E")
```

### เกณฑ์ประเมินการปฏิบัติการ

| รายการ | คะแนน |
|---|---:|
| สร้างตารางและ Hash Function ได้ถูกต้อง | 2 |
| เพิ่มข้อมูลและจัดการ Collision ได้ | 2 |
| ค้นหาและลบข้อมูลได้ถูกต้อง | 2 |
| แสดง Slot และ Load Factor ได้ | 2 |
| ทดสอบและอธิบายผลลัพธ์ได้ | 2 |
| **รวม** | **10** |

## แบบทดสอบหลังเรียน

เลือกคำตอบที่ถูกต้องที่สุด

1. ข้อใดอธิบายความสัมพันธ์ระหว่าง Hash Table และ Hash Function ได้ถูกต้อง

    - ก. Hash Table เรียงข้อมูล ส่วน Hash Function ลบข้อมูล
    - ข. Hash Table เก็บ Key-Value ส่วน Hash Function แปลง Key เป็นดัชนี
    - ค. Hash Table ใช้เฉพาะกับ Graph ส่วน Hash Function ใช้กับ Tree
    - ง. Hash Table และ Hash Function มีความหมายเหมือนกันทุกประการ

2. วิธีใดเป็นการจัดการ Collision แบบ Separate Chaining

    - ก. เก็บหลายรายการไว้ในรายการเดียวกันของ Slot
    - ข. ลบข้อมูลเดิมก่อนเพิ่มข้อมูลใหม่เสมอ
    - ค. เรียงข้อมูลทั้งหมดก่อนค้นหา
    - ง. ย้ายข้อมูลทุกค่าไปช่องสุดท้าย

3. วิธีใดเป็นการจัดการ Collision แบบ Open Addressing

    - ก. เก็บข้อมูลที่ชนกันใน Linked List ภายนอกตารางเท่านั้น
    - ข. ค้นหา Slot อื่นที่ว่างภายในตาราง
    - ค. ลบ Key ที่มีค่า Hash ซ้ำกัน
    - ง. เปลี่ยน Key ให้ไม่ซ้ำกันโดยอัตโนมัติ

4. เมื่อ Load Factor สูงขึ้น ผลที่มักเกิดขึ้นคืออะไร

    - ก. Collision ลดลงและค้นหาเร็วขึ้นเสมอ
    - ข. Collision เพิ่มขึ้นและการค้นหาอาจช้าลง
    - ค. Key ทุกค่าจะถูกลบออก
    - ง. ตารางจะเรียงข้อมูลตามลำดับเอง

5. เพราะเหตุใดการลบข้อมูลใน Open Addressing จึงต้องระวัง

    - ก. การลบอาจทำให้การค้นหาข้อมูลที่อยู่ถัดไปหยุดก่อนพบข้อมูล
    - ข. การลบทำให้ Hash Function เปลี่ยนชนิดข้อมูล
    - ค. การลบทำให้ทุก Slot กลายเป็น Collision
    - ง. การลบทำให้ตารางกลายเป็น Linked List เสมอ

6. Hash Table เหมาะกับงานประเภทใดมากที่สุด

    - ก. การค้นหาข้อมูลด้วย Key อย่างรวดเร็ว
    - ข. การแสดงข้อมูลตามลำดับจากน้อยไปมาก
    - ค. การท่องกราฟทุก Vertex
    - ง. การรักษาข้อมูลให้เรียงลำดับตลอดเวลา

### เฉลยแบบทดสอบหลังเรียน

1. ข
2. ก
3. ข
4. ข
5. ก
6. ก

## สรุปท้ายบท

Hash Table เป็นโครงสร้างข้อมูลแบบ Key-Value ที่ใช้ Hash Function แปลง Key เป็นตำแหน่งจัดเก็บ จึงทำให้การเพิ่ม ค้นหา และลบข้อมูลมีเวลาเฉลี่ย `O(1)` เมื่อการกระจายข้อมูลดี

Collision เป็นปัญหาที่เกิดเมื่อหลาย Key ได้ตำแหน่งเดียวกัน สามารถจัดการด้วย Separate Chaining, Linear Probing, Quadratic Probing หรือ Double Hashing การเลือก Hash Function ที่ดี การควบคุม Load Factor และการ Rehash ช่วยรักษาประสิทธิภาพของตารางแฮช

Hash Table เหมาะกับ Dictionary, Cache, การนับความถี่ และการตรวจสอบข้อมูลซ้ำ แต่ไม่รับประกันลำดับข้อมูลและมี Worst Case เป็น `O(n)` จึงควรเลือกใช้ให้เหมาะกับลักษณะของงาน
